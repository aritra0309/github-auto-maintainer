# Changelog

## Unreleased

- Phase 1 complete: deterministic task-based model routing and escalation with typed catalog/settings and strict tests.
- Phase 1.5 hardening: startup validation, lazy routing policy initialization safety, cost-aware deterministic tie-breaks, and regression coverage.
- Add multi-provider foundation scaffold (Python 3.12+, provider-ready layout, model catalog).
- Add Makefile targets (`test`, `lint`, `run`, `run-local`) and pre-commit hooks.
- Rename project package metadata to `github-maintainer`.
