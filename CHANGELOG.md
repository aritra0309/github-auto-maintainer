# Changelog

## Unreleased

- Add multi-provider foundation scaffold (Python 3.12+, provider-ready layout, model catalog).
- Add Makefile targets (`test`, `lint`, `run`, `run-local`) and pre-commit hooks.
- Rename project package metadata to `github-maintainer`.
